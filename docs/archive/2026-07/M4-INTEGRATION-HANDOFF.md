# M4 production integration

## Shipped runtime contract

- Watchers and the chase pursuer keep their stable actor IDs and exact four-watcher roster, but render `constable-rigged` with no primitive fallback.
- `townhouse` is the distinct Boston Watch House (`bldg-townhouse-civic` reuse), with imported steps, textured imported hanging-sign carrier, `INSPECTOR_OFFICE`, and `INSPECTOR_OFFICE_RELEASE` anchors. The Custom House remains separate.
- B11 uses imported `effigy-oliver`, `effigy-boot`, `organizer-crate-perch`, `protest-torch`, and `protest-banner`. Placard/banner art mounts on verified named nodes; motion uses the verified carry/hang/swing/sway/flame pivots. Only lights, smoke, and distant glow remain procedural.
- The minimal roof network is exactly two imported support boards. They export measured collision platforms and are reached with existing Shift+Space ballistics. `BALANCE`, `MANTLE`, and `JUMP_GAP` remain disabled and expose no misleading F prompt.
- B2/reprint/B12 commit a typed atomic `PRINT_JOB` with catch, ink, register, pull, and peel scores. Runtime recomputes quality, persists it in `world.printJobs`, and clamps accessible completion to usable or better. Thomas haul and notice posting require every authored phase.
- Remaining side jobs, three bounded challenges, information figures, knowledge mounts, four untracked eavesdrops, and flavor interactions use the unified F registry. Micros commit only after a deliberate completed read/exchange.

## Asset and accessibility limits

- `street-dog` is intentionally static. It may bark and react through text/audio, but never locomotes; the approved pipeline has no quadruped animation path.
- The crier uses `argu1` plus attributed subtitles. No human shout audio is fabricated because the available audio key lacks `sound_generation`.
- Gull flavor uses the approved gull cry plus an attributed text equivalent. No procedural bird body is rendered; a visible flock waits for an imported bird asset.
- The operable press v2 supplies six verified mechanical clips and imported paper staging. `printer-ink-balls.glb` is integrated into B2/reprint/B12: the named tools attach to the first-person hands, alternate around authored pivots, and drive deterministic ink coverage on the press while the explicit stroke control governs scoring.
  - Two imported mesh nodes `InkBall_Left` / `InkBall_Right` (instanced, shared geometry), grounded rest pose +Y up (handle up, ink pad down, pad contact at y=0), each ball ~0.30 m tall (pair footprint ~0.38 x 0.16 m).
  - Handle-aligned centerline pivots per ball: `InkBall_<Side>_grip` (handle top; hand attach / dab handle pivot), `InkBall_<Side>_rock` (pad-contact origin; rock pivot), and `InkSurface_<Side>` (ink-loaded leather face; ink-transfer / decal / FX anchor). Drive dab as a downward translate on the ball node and rock as a rotation about `_rock`; there is no armature.

## Final closure evidence

- Immutable QA matrix: `test-results/m4-final-close/report.json` (29 screenshots, 14 scenarios, non-black validation, zero game/page/HTTP-asset/network failures).
- Canonical print: B2, accessible B2, Pike reprint, and B12 all complete with imported ink tools and persistent composite quality.
- M1/M2 interrupt regression: synthetic `PA.FIELD.INTERRUPT.*` envelopes inherit authored choreography readiness, preserving live chase camera/input and confrontation decisions.
- Optimized exterior: `test-results/m4-final-performance-optimized/report.json`; authored M4 views remain below the ~350-call budget, with warm p50 frame times around 8.2–8.3 ms.
- Approved non-blocking limits remain unchanged: the dog is static, crier speech is subtitle-only, and gull flavor has audio/text without a procedural bird body.
  - Collision profile `none` (hand-held first-person tool, never a world collider). ~5.6k rendered triangles combined, embedded JPEG textures, 359 KB GLB.
